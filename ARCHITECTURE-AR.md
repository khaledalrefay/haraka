# مراجعة وتنظيف معمارية حركة — beta7

## النتيجة
المشروع أصبح أبسط في الصيانة وأوضح في فصل المسؤوليات، مع الحفاظ على سلوك beta6. لا أوصفه بأنه «مثالي» أو معتمد للإنتاج بلا اختبار هاتف؛ هذه بنية عملية لتطبيق صغير، وليست إضافة طبقات لمجرد تطبيق اسم Clean Architecture.

## تنظيم المسؤوليات
| المسار | المسؤولية |
|---|---|
| `site/js/domain/engine.mjs` | تحويل الخطة إلى خطوات، انتقالات الجلسة وحساب الوقت؛ بلا DOM أو تخزين |
| `site/js/domain/schedule.mjs` | الأيام والخطط والتعويض والاستحقاق |
| `site/js/domain/models.d.ts` | عقود موثقة للإعدادات والخطوات والجلسة وواجهة المستودع؛ لا تغيّر JavaScript إلى TypeScript ولا تستبدل التحقق أثناء التشغيل |
| `site/js/application/workout-service.mjs` | تسلسل الأوامر والتنسيق مع مستودع مُمرّر عبر constructor |
| `site/js/application/backup.mjs` | التصدير المنطقي والتحقق من بنية ومرجع النسخ الاحتياطية |
| `site/js/infrastructure/` | IndexedDB والصوت والتنزيل واتصال التذكيرات وحارس الإشعارات |
| `site/js/presentation/` | عرض الجلسة والمكتبة وأدوات النص الآمن وتنسيق الهدف |
| `site/js/data/` | المحتوى وتقديرات المدد والتسميات والألوان والأصوات ورقم الإصدار |
| `site/js/app.mjs` | نقطة تركيب الاعتماديات وتنسيق التنقل والأحداث وشاشات الإعدادات والسجل والمقارنة |
| `reminder-server/` | API التذكير، قواعد الإرسال، تشفير Web Push وD1 |
| `dist/` | ناتج نشر محدود بأصول التشغيل فقط |

منطق التطبيق لا يعتمد على المستودع الملموس؛ خدماته تستقبل المستودع من نقطة التركيب. التحقق من النسخ الاحتياطية لم يعد يستورد تنفيذ Web Audio، وتنزيل الملف منفصل عن التحقق. فحص المعمارية يتحقق من وصول نقطة الدخول إلى وحدات JS العشرين ومن حدود الاعتماد بين المنطق والخدمات.

فصل العرض هنا تدريجي ومتناسب: ما زال منسّق التطبيق يحتوي قوالب الإعدادات والسجل والمقارنة، ومهايئ التذكيرات يحتوي عناصر واجهته. هذا ليس فصلًا كاملًا لكل شاشة وكل مؤثر جانبي. يمكن استخراجهما عند توسع هذه الميزات؛ لا يحتاج التشغيل الحالي إلى Framework أو DI container أو مستودع منفصل لكل حقل.

## البيانات وقواعد التخزين
| المخزن | البيانات والقرار |
|---|---|
| IndexedDB `haraka-vnext`، الإصدار 1 | الإعدادات والجلسة الجارية والسجل. أبقينا الاسم والإصدار عمدًا؛ تغيّر تنظيم الملفات لا يستلزم ترحيل البيانات |
| `settings/preferences` | إعدادات واحدة، مستويات البرامج وتاريخ مراجعات الجدول |
| `active/current` | جلسة جارية واحدة ونسخة محتواها ومؤقتها ومراجعتها |
| `history` بمفتاح معرف الجلسة | الجلسات المنتهية أو المتوقفة؛ حذف صريح يسمح بإعادة الموعد |
| IndexedDB `haraka-reminder-guard`، الإصدار 1 | نسخة صغيرة يقرأها Service Worker لمنع إشعار غير مناسب عند إغلاق الواجهة |
| localStorage `haraka-reminders-v1` | حالة الجهاز واعتمادات التذكير المحلية؛ لا تدخل النسخ الاحتياطية العامة |
| D1 `devices` | تسجيل الأجهزة والتفضيلات وحالة الإرسال؛ لا يُحفظ سجل التدريب الكامل في السحابة |

التخزين يحفظ انتهاء الجلسة وانتقالها من الجارية إلى السجل ضمن معاملة واحدة. رقم revision يمنع الكتابة فوق تعديل من تبويب آخر. snapshot الأصلية تبقى مرجع الجرعة ولا تُستبدل بمحتوى أحدث أثناء الحفظ. أسماء ومفاتيح التخزين وصيغة `haraka-v2` وإصدار المحتوى لم تتغير.

الإبقاء على snapshot لكل جلسة مقصود للحفاظ على التاريخ؛ يؤدي إلى تكرار بعض البيانات، لكنه أفضل هنا من ترابط السجل مع خطة قابلة للتغيير. لا توجد فهارس تاريخية في قاعدة المتصفح حاليًا: تحميل السجل كاملًا مناسب للنطاق الحالي؛ إذا نما لآلاف الجلسات باستمرار فستحتاج القائمة إلى pagination وفهارس. D1 لديه فهرس `(enabled,id)` لمعالجة الأجهزة المفعّلة. لم نعد بناء قاعدة فعلية أو نحذف سجلات مستخدم.

## ما أزيل أو نُظّف
- التطبيق القديم: ملفات `core` و`features` وبيانات الخطط القديمة، والـbundle القديم وصفحة HTML المكررة و`legacy-index-v1.html` وCSS الشاشة القديمة غير المحمّلة.
- نسخ اختبارات UI المتراكمة beta2–beta5؛ الاحتفاظ بالاختبار الحالي واختبارات التخزين والنسخ الاحتياطية وPWA والخادم.
- تقارير الإصدارات والتعليمات القديمة المتضاربة؛ استبدالها بدليل تشغيل ومراجعة حاليين. بقي المرجع المعتمد السابق كسجل قرارات، وليس تعليمات بناء لهذه الحزمة.
- طريقة `hideRecord` غير المستخدمة من كود الإنتاج؛ اختبار البيانات المخفية السابقة ينشئ حالته داخل الاختبار فقط.
- 46 تصريح CSS مستبدل بتصريحات لاحقة للمنتقي والشرط نفسيهما، دون نقل ترتيب القواعد؛ مع إزالة منتقيات واجهة لم تعد موجودة وتنسيق الملفات للقراءة.
- 16 صورة غير مستعملة حاليًا نُقلت إلى `artwork`، ولم تُحذف. عينة `march.jpg` بقيت ضمن أصول العرض والكاش.
- إصلاح رقم الإصدار العالق على beta3. المصدر الآن package.json، والبناء يولّد رقم الواجهة وبصمة الكاش.

## البناء وPWA
يوجد HTML مصدر واحد وbundle تشغيل واحد. البناء يولّد المعاينة المستقلة ومجلد `dist` وقائمة اعتماديات البناء. أوامر البناء تستخدم Node API لتفادي مشكلة مشغلات npm على Windows. esbuild وPlaywright مثبتان بإصدارات محددة في ملف القفل.

يبقى التطبيق cache-first بصدفة تشغيل موحدة، وتنتظر النسخة الجديدة إغلاق صفحات النسخة السابقة. كل أصول الصدفة المطلوبة موجودة في precache؛ أي تغير فيها يغيّر البصمة. الاختبارات والمصادر والصور غير المدمجة لا تدخل `dist`. خدمة التذكيرات منفصلة، واختبار مسارها لا يعني اختبار التسليم الفعلي إلى الهاتف.

الملفات المولدة ليست كودًا ميتًا: `app.bundle.js` و`Haraka-Preview.html` و`dist` نواتج مقصودة. عدّل المصادر ثم أعد البناء. لا تعدّل المولدات يدويًا.

## التحقق المنفذ
- 17 اختبار منطق؛ منها مطابقة الخطط الـ45 والجرعات والترتيب والجدولة وتغيير البرامج والأيام والاستعادة.
- IndexedDB في Chromium: إعادة الفتح وتعارض التبويبات والنقر المزدوج والحفظ الذري وحراسة المواعيد.
- اختبار UI الحالي: الفئات الخمسون دون تكرار، 24 حالة أسماء وجهات على 3 مقاسات، ثبات زر الإنهاء، مناطق لمس 44 بكسل وسلاسة المؤقت.
- مقارنة خمس لقطات ثابتة قبل تنظيف CSS وبعده: تطابق بكسلي، بما فيها المكتبة والجلسة والشاشة الصغيرة.
- اختبار الترقية الحقيقي beta6 → beta7 عبر Service Worker على origin واحد: بقيت الإعدادات والسجل والجلسة الجارية متطابقة عند مقارنة JSON، وقُبلت نسخة beta6 الاحتياطية واستؤنف المؤقت المتوقف.
- اختبار offline وإعادة التحميل، انتظار وتفعيل تحديث العامل، المعاينة المستقلة والصورة المضمنة، وتصدير واستعادة البيانات.
- اختبارات خدمة التذكيرات: قواعد التوقيت والمنع، API ومصادقة وSQLite، منع التكرار وإعادة المحاولة وتشفير Web Push وتوقيع VAPID.
- تثبيت اعتمادات من ملف القفل ثم بناء مستقل ناجح.

## الحدود المتبقية
لم يتم النشر أو اختبار هاتف فعلي أو ترقية D1 مستضاف. اختبار إرسال التذكيرات من الواجهة يستخدم نقلًا محاكى؛ التسليم الحقيقي يحتاج تجربة النسخة المستضافة. هذه ليست مراجعة أمنية خارجية أو قياس أداء طويل الأمد. الصور الفعلية وتدقيق المحتوى ومدد الخطط من أعمال المشروع السابقة وتبقى منفصلة عن هذا التنظيف.

النسخ الاحتياطية مرتبطة بإصدار المحتوى الحالي عمدًا؛ عند تغييره لاحقًا ينبغي تصميم مهاجر محتوى واضح، وليس فقط تبديل رقم الإصدار. إعدادات المستخدم لا تملك بروتوكول دمج بين التبويبات مثل revision الجلسة؛ آخر حفظ يفوز. لم نغيّر هذا السلوك في تنظيف beta7.
