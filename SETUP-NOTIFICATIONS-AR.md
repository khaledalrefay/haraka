# إعداد تذكيرات حركة — ابدأ هنا

الحزمة قسمان: site وهو الموقع كاملًا، وreminder-server وهي خدمة إرسال مستقلة. لا نملك صلاحية نشرها على حسابك؛ الخطوات التالية تُنفّذ عندك. رفع site وحده يحافظ على بقية التطبيق، لكنه لا يفعّل التذكيرات.

## 1. تجهيز خدمة الإرسال

ثبّت Node.js 24، وافتح PowerShell داخل مجلد reminder-server بعد فك الضغط. ثم:

```powershell
npm install
npx wrangler login
npx wrangler d1 create haraka-reminders
```

ستفتح خطوة login تسجيل الدخول إلى Cloudflare. احتفظ بمعرّف database_id الذي يرجع من أمر إنشاء قاعدة البيانات. افتح wrangler.jsonc وضعه مكان REPLACE_WITH_CREATED_DATABASE_ID، واترك binding باسم DB.

تأكد أن APP_ORIGIN هو رابطك الحالي بالضبط دون شرطة مائلة في النهاية:

https://haraka.khaled-refay98.workers.dev

اسم الخدمة الجديدة haraka-reminders مستقل عن خدمة موقعك. لا تغيّره إلى اسم Worker الموقع الحالي حتى لا تستبدل موقعك بالخادم.

ثم أنشئ الجدول:

```powershell
npx wrangler d1 execute haraka-reminders --remote --file schema.sql
```

## 2. توليد مفاتيح هذا التثبيت

استبدل YOUR_EMAIL بعنوان بريد تواصل مناسب لك، ثم:

```powershell
node scripts/generate-keys.mjs mailto:YOUR_EMAIL
npm run deploy
npx wrangler secret bulk .secrets.json
```

سيُنشأ ملف .secrets.json محليًا ويُرفع محتواه إلى أسرار Worker. يحتوي VAPID_PRIVATE_KEY وPAIRING_TOKEN، فلا ترفعه إلى GitHub ولا إلى ملفات الموقع ولا ترسله في محادثة. لا تعِد تشغيل توليد المفاتيح كلما حدّثت التطبيق؛ السكربت يرفض الكتابة فوق ملف موجود عمدًا.

احتفظ بالملف في مكان خاص عندك. من داخله ستحتاج قيمة PAIRING_TOKEN لإدخالها في التطبيق عند التفعيل. لا يحتاج التطبيق المفتاح الخاص.

دوّن رابط haraka-reminders الذي يظهر بعد deploy. قد تستغرق جدولة Cron الجديدة وقتًا حتى تصبح فعالة. هذه الأوامر لم تُنفذ على حسابك هنا؛ إذا ظهر خطأ توقف عنده وشارك نصه دون أسرار.

## 3. ربط الموقع

افتح site/config.js وضع رابط خدمة haraka-reminders المنشورة مكان النص الفارغ. مثال بالشكل فقط:

```js
window.HarakaConfig = {
  reminderApi: 'https://haraka-reminders.YOUR-SUBDOMAIN.workers.dev'
};
```

هذا رابط خدمة الإرسال، وليس رابط موقع حركة. لا تضف رمز التفعيل أو المفاتيح في هذا الملف.

ارفع محتويات site إلى جذر مستودع الموقع، ثم انتظر نشر Cloudflare المعتاد. لا ترفع مجلد الحزمة كاملًا ولا reminder-server. إذا كنت قادمًا من النسخة الأصلية احذف app.js وdata.js وstyle.css من جذر الموقع؛ البدائل موجودة داخل js وcss. لا توجد ملفات إضافية للحذف إن كنت رفعت المرحلة الرابعة.

بعد اكتمال النشر أغلق كل نوافذ حركة والتطبيق المثبت، ثم افتحه ثانية لتفعيل Service Worker الجديد. لا تمسح بيانات الموقع. عند تحديث ملفات مستقبلًا غيّر VERSION في sw.js.

## 4. تفعيل الهاتف

1. افتح الموقع الأصلي المثبّت من Chrome على أندرويد.
2. افتح الإعدادات واختر الساعة ثم «تفعيل التذكير».
3. أدخل PAIRING_TOKEN من ملفك الخاص ووافق على إذن المتصفح.
4. افتح الإعدادات واضغط «تجربة الإشعار». نجاح الطلب يعني أن المزود قبله، وليس إثباتًا لوصوله؛ تحقق من ظهوره على الهاتف.
5. أغلق التطبيق وجرب تذكيرًا مجدولًا في يوم تدريب، بوقت بعد عدة دقائق. لا يكفي نجاح تجربة التطبيق مفتوحًا لإثبات الخلفية.
6. اختبر إكمال جلسة قبل وقتها وأن التذكير لا يظهر، واختبر الإيقاف وتعديل الأيام.

في حال حظر الإذن، فعّله من إعدادات إشعارات الموقع والمتصفح/الهاتف. إن تعذر التسجيل مع تحديث ينتظر، أغلق كل نوافذ الموقع وافتحه ثانية. تجربة الإشعار محدودة بمرة كل دقيقة.

## حدود متوقعة

- التذكير يحتاج إنترنت؛ ليس منبهًا محليًا دقيقًا بالدقيقة.
- محاولات الإرسال لمدة خمس دقائق بعد الساعة المختارة، ثم تُترك إلى اليوم المستحق التالي.
- لا توجد إشعارات في أيام الراحة، ولا تذكيرات متراكمة.
- عند الإنجاز دون اتصال يُحفظ منع محلي وتُزامن الخدمة عند العودة؛ لا يمكن ضمان سباق وصول إشعار في نفس لحظة الإنجاز أو سلوك كل جهاز.
- كل جهاز له تسجيل مستقل. نقل السجل إلى متصفح آخر لا ينقل إذن الإشعارات أو رموز الجهاز.
- إيقاف التذكير يعطله محليًا ويطلب تعطيله على الخادم. عند انقطاع الاتصال يظل الطلب بحاجة للمزامنة.

## الفحص المحلي للمطور

داخل reminder-server:

```powershell
node tests/server.mjs
```

داخل site:

```powershell
node --experimental-vm-modules tests/reminders.cjs
node tests/push-handler.cjs
node tests/offline.cjs
```

اختبارات SQLite والتشفير والمحاكاة اجتازت محليًا؛ نشر Cloudflare ووصول هاتفك لم يُختبرا بعد.

مراجع رسمية للتنفيذ:

- https://developers.cloudflare.com/workers/configuration/cron-triggers/
- https://developers.cloudflare.com/d1/get-started/
- https://developers.cloudflare.com/workers/configuration/secrets/
- https://developer.mozilla.org/en-US/docs/Web/API/Push_API
- https://www.rfc-editor.org/rfc/rfc8291
- https://www.rfc-editor.org/rfc/rfc8292
