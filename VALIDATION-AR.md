# فحص الإصدار 1.0.0

نجح البناء وفحص المتصفح Chromium بعرض 390 بكسل: بدء سجل نظيف، حفظ الإعدادات، الباليتات السبع، اختيار النغمات، تبديل أيام الجلسات، الصور والشرح، مؤشر الخطوات والتخطي، راحة 25 ثانية، استئناف الجلسة بعد إعادة الفتح وحفظ نتيجتها. تمت مراجعة لقطات شاشة الجلسة والإعدادات والأيام بصريًا.

نجحت محاكاة التخزين دون اتصال لجميع الملفات الستين واختبارات منطق التذكيرات ومعالج الدفع. تفعيل التذكيرات على جهاز فعلي واستضافة HTTPS ما زال للمرحلة التالية. تشغيل النغمات برمجيًا لا يغني عن تجربة مستوى الصوت على جهاز المستخدم.

لإعادة فحص المتصفح: ثبّت Playwright وChromium ثم شغّل `node tests/release.cjs` من site. لفحوص التخزين والتذكيرات شغّل `node tests/offline.cjs` و`node tests/push-handler.cjs` و`node --experimental-vm-modules tests/reminders.cjs`.

## تحديث الألوان 1.0.1
نجح فحص الباليتات التسع بالوضعين الفاتح والداكن، وحفظ الاختيار بعد إعادة الفتح. فُحص تباين ألوان النص الأساسية والثانوية والأزرار؛ أدنى نسبة في الأزواج المفحوصة 5.42:1. تمت مراجعة لقطات الإعدادات على عرض 390 بكسل في الوضعين. نجح اختبار الجلسات والإعدادات، وجميع مسارات التخزين المؤقت الـ61. لا يغيّر التحديث مفتاح حفظ بيانات الإصدار الأول أو مخططها.
