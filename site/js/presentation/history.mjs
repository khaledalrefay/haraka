import { pageHeading } from './page-heading.mjs';
import { executionDate } from "../domain/schedule.mjs";
import { button, esc } from './html.mjs';
import { levelName, sessionName } from '../data/program-labels.mjs';
import { icon } from './icons.mjs';
export function renderHistory(history, month, hidden) {
 const rows = history.filter(r=>!r.hidden && executionDate(r).startsWith(month)).sort((a,b)=>b.finishedAt-a.finishedAt);
 return `${pageHeading("سجلّي")}<div class="v-month-nav">${button('→','history-prev','icon-btn','aria-label="الشهر السابق"')}<label class="v-month-picker"><span>${new Intl.DateTimeFormat("ar", {month:"long",year:"numeric",timeZone:"UTC",numberingSystem:"latn"}).format(new Date(month+"-01T12:00:00Z"))}</span><input aria-label="اختيار الشهر" type="month" id="history-month" value="${month}"></label>${button('←','history-next','icon-btn','aria-label="الشهر التالي"')}</div>${rows.length ? rows.map(r=>{const w=r.snapshot.workout;return `<article class="card record v-compact-record"><button class="v-record-main" data-action="record" data-id="${esc(r.id)}" aria-label="تفاصيل ${esc(sessionName(w.program,w.session))}"><span class="v-record-title"><strong>${esc(sessionName(w.program,w.session))}</strong><small>${r.status==='completed'?'✓ مكتملة':'انتهاء مبكر'}</small></span><span class="small muted">${new Intl.DateTimeFormat('ar', {day:'numeric',month:'long',year:'numeric',timeZone:'UTC',numberingSystem:'latn'}).format(new Date(executionDate(r)+'T12:00:00Z'))} · ${levelName(w.level)}</span></button><div class="v-record-actions">${button(icon('trash'),'delete-record','icon-btn v-delete',`data-id="${esc(r.id)}" aria-label="حذف الجلسة"`)}</div></article>`}).join('') : '<section class="card empty"><p>لا توجد جلسات مسجّلة في هذا الشهر.</p></section>'}${hidden}`;
}
