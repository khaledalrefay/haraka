import test from 'node:test';import assert from 'node:assert/strict';
import {changePlan,opportunity,slotFor,todayKey} from '../../js/domain/schedule.mjs';
const plan={program:'move',level:1,schedule:[{day:0,session:'A'},{day:2,session:'B'},{day:4,session:'C'}]};
test('plan changes preserve current slot and makeup, apply to next day onward',()=>{let s=changePlan({revisions:[]},plan,'2026-09-20');s=changePlan(s,{...plan,program:'strength',level:3},'2026-09-22');assert.equal(slotFor(s,'2026-09-22').program,'move');assert.equal(opportunity(s,[],'2026-09-23').program,'move');assert.equal(slotFor(s,'2026-09-24').program,'strength');});
test('only first rest day offers makeup; no carry into new due slot or before onboarding',()=>{const s=changePlan({revisions:[]},plan,'2026-09-20');assert.equal(opportunity(s,[],'2026-09-21').kind,'makeup');assert.equal(opportunity(s,[],'2026-09-24').kind,'scheduled');assert.equal(opportunity(s,[],'2026-09-25').kind,'makeup');assert.equal(opportunity(s,[],'2026-09-26'),null);assert.equal(opportunity(s,[],'2026-09-19'),null);});
test('recorded occurrence and performed day both block repeat; dates use Damascus',()=>{const s=changePlan({revisions:[]},plan,'2026-09-20');assert.equal(opportunity(s,[{dateKey:'2026-09-21',scheduledDate:'2026-09-20'}],'2026-09-21'),null);assert.equal(opportunity(s,[{dateKey:'2026-09-20',scheduledDate:'2026-09-20'}],'2026-09-21'),null);assert.equal(todayKey(new Date('2026-09-23T22:00:00Z')),'2026-09-24');});
